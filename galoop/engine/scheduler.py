"""
galoop/engine/scheduler.py  — DEPRECATED

Job submission is now handled by row (workflow.toml + `row run`).
The _relax CLI command is invoked by row for each pending job.

This module is kept as a stub so existing imports don't break.
"""
